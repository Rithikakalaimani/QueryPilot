# Minimal deps for Railway (no torch, no sentence-transformers, no ragas).
# Use EMBEDDING_PROVIDER=openai and OPENAI_API_KEY. RAGAS /evaluate is disabled.

fastapi==0.109.2
uvicorn[standard]==0.27.1
python-dotenv==1.0.1
pydantic==2.6.1
pydantic-settings==2.1.0

# Database
pymysql==1.1.0
cryptography>=41.0.0
psycopg2-binary==2.9.9
SQLAlchemy==2.0.25

# Embeddings: OpenAI only (no sentence-transformers)
openai==1.12.0
faiss-cpu>=1.8.0

# LLM
groq>=0.4.0

# SQL parsing
sqlparse==0.5.0

# Redis (optional)
redis>=5.0.0

# Utils
httpx==0.26.0
numpy==1.26.4
